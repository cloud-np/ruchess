use ruchess_cli::Args;
use ruchess_core::{Rank, Square};
use ruchess_core::fen::parse_fen;

fn main() {
    // let args = Args::init();
    // parse_fen(&args.fen);
    
    println!("{}", Square::C1);
}
